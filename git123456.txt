outlook 테스트 케이스 (App/Web)

--> 첨부 파일 1개일 경우 테스트 (구분을 위해 제목/본문 내용 다르게 작성)
  
  > 시간을 두고 전송 (디코더 처리 확인 후)
  > 같은 파일을 전송 후 바로 전송
  > 다른 파일을 전송 후 바로 전송
  > 파일크기를 다르게 전송
  > 각 다른 파일 확장자로 전송 ( ex) jpg,png,xlsx,docx..... )
  > 같은 파일을 파일명만 바꿔 전송 후 바로 전송
  > 

--> 첨부 파일 다랑(5개 이상)일 경우 테스트 (구분을 위해 제목/본문 내용 다르게 작성)

  > 시간을 두고 전송 (디코더 처리 확인 후) 
  > 같은 파일들로 전송 후 바로 전송      
  > 다른 파일들로 전송 후 바로 전송
  > 같은 파일을 파일명만 바꿔 전송 후 바로 전송
  >  
 